<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns every pixel; this theme only controls the window before the first frame. -->
    <style name="Theme.OpenHelm" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/openhelm_window_background</item>
        <item name="android:statusBarColor">@color/openhelm_window_background</item>
        <item name="android:navigationBarColor">@color/openhelm_window_background</item>
    </style>
</resources>
