<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sockets to the MFD, and NsdManager discovery. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- bindProcessToNetwork: the process must be pinned to the Wi-Fi network before any socket
         opens, or traffic routes over cellular and the MFD is unreachable. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Multicast lock: many devices drop mDNS multicast unless one is held during discovery. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Backup is allowed *deliberately*, with explicit rules rather than the default sweep:
         the only state worth carrying to a new phone is the DataStore (remembered displays,
         their names, the chosen palette), and the rules say exactly that. -->
    <application
        android:name=".OpenHelmApplication"
        android:label="OpenHelm"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.OpenHelm"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
